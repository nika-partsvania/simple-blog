import Parent from "./component/parent";

const TestView = () => {
  return (
    <div style={{ minHeight: "100vh", padding: 100 }}>
      <Parent> </Parent>
    </div>
  );
};

export default TestView;
